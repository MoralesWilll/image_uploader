<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl"><%= t("images.new") %></h1>

  <%= render "form", image: @image %>

  <%= link_to t("images.back_to_images"), images_path(locale: I18n.locale), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
